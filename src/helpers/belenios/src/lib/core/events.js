// Generated by Melange

import * as Caml_option from "melange.runtime/caml_option.js";

function update_roots(h, $$event, accu) {
  var match = $$event.event_typ;
  if (match === "EncryptedTally") {
    return {
            roots_setup_data: accu.roots_setup_data,
            roots_last_ballot_event: accu.roots_last_ballot_event,
            roots_encrypted_tally: $$event.event_payload,
            roots_last_shuffle_event: accu.roots_last_shuffle_event,
            roots_last_pd_event: accu.roots_last_pd_event,
            roots_result: accu.roots_result
          };
  } else if (match === "PartialDecryption") {
    return {
            roots_setup_data: accu.roots_setup_data,
            roots_last_ballot_event: accu.roots_last_ballot_event,
            roots_encrypted_tally: accu.roots_encrypted_tally,
            roots_last_shuffle_event: accu.roots_last_shuffle_event,
            roots_last_pd_event: Caml_option.some(h),
            roots_result: accu.roots_result
          };
  } else if (match === "Ballot") {
    return {
            roots_setup_data: accu.roots_setup_data,
            roots_last_ballot_event: Caml_option.some(h),
            roots_encrypted_tally: accu.roots_encrypted_tally,
            roots_last_shuffle_event: accu.roots_last_shuffle_event,
            roots_last_pd_event: accu.roots_last_pd_event,
            roots_result: accu.roots_result
          };
  } else if (match === "Setup") {
    return {
            roots_setup_data: $$event.event_payload,
            roots_last_ballot_event: accu.roots_last_ballot_event,
            roots_encrypted_tally: accu.roots_encrypted_tally,
            roots_last_shuffle_event: accu.roots_last_shuffle_event,
            roots_last_pd_event: accu.roots_last_pd_event,
            roots_result: accu.roots_result
          };
  } else if (match === "Shuffle") {
    return {
            roots_setup_data: accu.roots_setup_data,
            roots_last_ballot_event: accu.roots_last_ballot_event,
            roots_encrypted_tally: accu.roots_encrypted_tally,
            roots_last_shuffle_event: Caml_option.some(h),
            roots_last_pd_event: accu.roots_last_pd_event,
            roots_result: accu.roots_result
          };
  } else if (match === "Result") {
    return {
            roots_setup_data: accu.roots_setup_data,
            roots_last_ballot_event: accu.roots_last_ballot_event,
            roots_encrypted_tally: accu.roots_encrypted_tally,
            roots_last_shuffle_event: accu.roots_last_shuffle_event,
            roots_last_pd_event: accu.roots_last_pd_event,
            roots_result: $$event.event_payload
          };
  } else {
    return accu;
  }
}

var empty_roots = {
  roots_setup_data: undefined,
  roots_last_ballot_event: undefined,
  roots_encrypted_tally: undefined,
  roots_last_shuffle_event: undefined,
  roots_last_pd_event: undefined,
  roots_result: undefined
};

export {
  empty_roots ,
  update_roots ,
}
/* No side effect */
